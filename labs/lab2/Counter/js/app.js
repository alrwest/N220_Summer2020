// Creating a counter app with p5.js

// Declaring global variable "counter"
// Setting variable's value to be 0 
// Good note to remember: variables can be named almost anything
let coolCounter = 0;

// Creating draw function
// Modifying variable "counter" to add one to add one to it's value

function draw() { 
    coolCounter = coolCounter + 1;
    console.log(coolCounter);
} 